
#ifndef _BOOTACODDY_UTIL_MATH_H
#define _BOOTACODDY_UTIL_MATH_H

#include "util/types.h"

namespace bac::math {

    i32 abs(i32 num);

    i32 pow(i32 num, i32 power);

    u32 max(u32 a, u32 b);

    u32 min(u32 a, u32 b);

}

#endif